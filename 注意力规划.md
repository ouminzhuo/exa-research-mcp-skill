建议 skill 运行顺序是：

```
1. 生成 candidate_project_pool2. 生成 project_ledger3. 生成 capacity_reconciliation4. 生成 participant_ledger5. 生成 source_trace / evidence_table6. 生成 detailed_project_cards7. 生成 full_report 正文8. 生成 lite_report
```

也就是说：

```
先有账本，再写正文；先有原版，再抽lite；先有证据，再下判断。
```

## 全量版报告升级

该次升级有没有满足如下目标：

1. skill 提示词：重新定义报告产品形态和写作边界  
2. JSON schema：固化项目账本、项目卡、证据表字段  
3. JS 调度器：强制执行生成顺序、容量反算、字段完整性和证据边界  
4. 搜索策略：保持既有高召回，不要继续无限扩展

**提示词负责“报告应该长什么样、不能写成什么样”；JS 调度器负责“有没有真的按这个样子生成”。**

换句话说：

```
Prompt = 方向、定位、写作规则JS scheduler = 流程、字段、校验、禁止跳步Schema = 数据结构Report writer = 从账本生成正文
```

## 1. skill 提示词需要升级什么

提示词里最重要的是把报告定位写死：

```
这不是策略建议书，不是 lite 版，而是国别风电市场现状描述母版。
```

同时要写死三条边界：

```
1. 只描述市场现状，不替读者做最终决策。2. 正文可以摘要，但项目卡和项目账本不能压缩事实字段。3. 明阳相关性只能作为事实相关性/潜在线索描述，不写成行动建议。
```

你现在特别要防止模型继续犯三个错：

| 错误           | 表现                     | 解决方式                  |
| ------------ | ---------------------- | --------------------- |
| 把全量报告写成 lite | 项目卡变薄、机型/融资/SPV/PPA被省略 | 项目卡完整字段硬规则            |
| 把现状描述写成策略建议  | “建议建厂”“应绑定某EPC”        | 禁止进入最终决策              |
| 把机会容量写成项目容量  | 项目 1.5GW 全算作机会         | `Opportunity MW` 单独字段 |
## 2. JSON schema 需要升级什么

你这个 V4 最大变化是：**报告正文不再承担全部信息存储功能，很多东西应该进入 JSON 数据库。**

所以要把以下内容结构化：

```
project_ledger.jsonproject_cards.jsonparticipant_ledger.jsonoem_competition.jsonprocurement_window.jsonevidence_table.jsonrisk_matrix.jsoncapacity_reconciliation.jsontracking_watchlist.json
```

其中最重要的是三个 schema。

### A. `project_ledger.json`

这个是全量报告的数据库核心。你现在列的字段已经够好，建议强制每个项目必须有：

```
Project IDAlias Group ID规范项目名容量 MWOpportunity MW项目阶段Status Basis是否计入确认容量是否计入机会容量开发商/业主SPV股权结构OEMProcurement Status机型EPCO&M主体融资方PPA/承购方电价COD/时间线Next MilestoneDecision Maker关键证据Field Confidence待核事项明阳相关性主要风险
```

### B. `project_cards.json`

项目卡不能从正文表格临时生成，而应该是独立对象。这样正文再怎么压缩，项目卡都不会变薄。

### C. `evidence_table.json`

证据表必须支持“结论级证据”，不是简单 URL 列表。

字段建议：

```
ConclusionConclusion Type: fact / inference / estimate / judgment / pendingSourceSource GradeDirectly Proves: yes / partially / noConfidenceRelated FieldPending Verification Action
```

这能防止“来源强，所以所有结论都强”的误判。

---

## 3. JS 调度器需要升级什么

JS 调度器不需要再增加很多搜索 worker，当前重点是 **生成顺序和质量门禁**。

建议改成这条流水线：

```
1. candidate_project_pool2. project_ledger3. capacity_reconciliation4. detailed_project_cards5. participant_ledger6. oem_competition_matrix7. procurement_window_table8. evidence_table9. full_report10. lite_report
```

不要让模型直接写报告。必须先生成账本，再写正文。

## JS 必须加的 gate

### 1. `project_card_completeness_gate`

检查每个重点项目卡是否包含完整字段。字段未知可以写“待核”，但不能消失。

```
如果项目卡缺少 SPV / OEM / 机型 / 融资 / PPA / EPC / COD / 证据边界 等字段，不得进入最终报告生成。
```

### 2. `capacity_reconciliation_gate`

所有容量必须从项目账本反算：

```
确认容量 = 已投运 + 在建 + 融资关闭 + 其他定义内阶段机会容量 = Opportunity MW 求和观察名单容量 = watchlist 求和国家目标容量 = 单独列，不得混入项目管线
```

### 3. `no_strategy_recommendation_gate`

扫描报告正文，避免出现过度策略化表达：

```
应当建厂必须进入建议报价必须绑定某EPC应该投入资源
```

可以替换为：

```
当前已有本地化布局该项目与明阳存在相关性该信息需进一步核实该采购窗口尚未公开确认
```

### 4. `evidence_boundary_gate`

所有关键判断必须有 evidence table 记录，尤其是：

```
项目阶段容量OEM中标电价融资关闭PPA签署采购窗口明阳相关性
```

### 5. `baseline_inheritance_gate`

历史版本中已出现的项目不能静默消失。必须进入：

```
confirmedwatchlistduplicaterejectedunresolved
```

---

## 4. 当前不用大改搜索策略

搜索策略现在只需要保留这句：

```
广搜，不广算；所有候选先入 candidate_project_pool，最终是否入账由证据和阶段规则决定。
```

不要继续把搜索入口无限扩展。当前主要矛盾已经不是“没搜到”，而是：

```
搜到的信息如何进入账本；账本如何生成项目卡；项目卡如何不被正文压缩；证据如何限制结论边界。
```

## 5. 最终升级方案

你现在应该这样改：

| 模块                   | 是否需要改 | 改什么                                               |
| -------------------- | ----- | ------------------------------------------------- |
| Skill 主提示词           | 需要    | 写入 V4 报告定位、章节结构、非策略建议边界                           |
| Report Writer prompt | 强烈需要  | 强制“正文摘要 + 项目卡完整附录”                                |
| JSON schema          | 强烈需要  | 固化 project ledger / project card / evidence table |
| JS 调度器               | 强烈需要  | 加 capacity、project card、evidence、no-strategy gate |
| 搜索策略                 | 小改即可  | 保留高召回，不再无限扩展                                      |
| Lite 版生成             | 后置    | 必须从 full report 抽取，不能直接生成                         |

最终执行原则：

```
提示词定义 V4 报告产品；JSON 固化数据结构；JS 调度器保证不跳步、不压缩、不乱算；报告正文只负责表达现状。
```

V4 的“报告定位和章节结构”放进 skill 提示词；“字段完整、容量反算、证据边界、禁止策略化”交给 JS 调度器强制执行。